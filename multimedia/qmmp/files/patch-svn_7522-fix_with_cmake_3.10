https://sourceforge.net/p/qmmp-dev/code/7522/

--- CMakeLists.txt
+++ CMakeLists.txt
@@ -8,6 +8,10 @@
 IF(${CMAKE_SYSTEM_NAME} MATCHES "Darwin")
 	SET(CMAKE_REQUIRED_INCLUDES ${CMAKE_REQUIRED_INCLUDES} /opt/local/include)
 ENDIF()
+
+if(POLICY CMP0071)
+  cmake_policy(SET CMP0071 NEW)
+endif()

 #extract version from qmmp.h
 FILE(STRINGS "${CMAKE_CURRENT_SOURCE_DIR}/src/qmmp/qmmp.h"
--- src/plugins/General/hotkey/CMakeLists.txt
+++ src/plugins/General/hotkey/CMakeLists.txt
@@ -61,6 +61,7 @@
 include_directories(${CMAKE_CURRENT_BINARY_DIR})

 IF(X11_FOUND)
+set_property(SOURCE hotkeymanager_x11.cpp PROPERTY SKIP_AUTOMOC ON)
 ADD_LIBRARY(hotkey MODULE ${libhotkey_SRCS} ${libhotkey_UIS_H} ${libhotkey_RCC_SRCS})
 add_dependencies(hotkey qmmpui)
 target_link_libraries(hotkey ${QT_LIBRARIES} qmmpui libqmmp ${X11_LDFLAGS})
